using UnityEngine;

public class CameraPlayerMovement : MonoBehaviour
{
    public int Time;

    // Update is called once per frame
	void Update ()
	{     
	   	//var player = GameManager.GetLeadingPlayer();
     	//iTween.MoveTo(gameObject, new Vector3(player.transform.position.x, gameObject.transform.position.y, gameObject.transform.position.z), Time);
	    
	   // Debug.Log(renderer.isVisible ? "Visible" : "Invisible");
	}
}
